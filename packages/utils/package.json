{
  "name": "utils",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "OUT_FILE='packages/utils/dist/index.js' PACKAGE_DIR='utils' rollup -c ../../rollup.config.js && npm run typegen",
    "typegen": "tsc"
  }
}
